{{#if model}}
  <table class="primary">
    <thead>
      <tr>
        <th>
          Description
        </th>
        <th>
          Borrowed Since
        </th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {{#each model as |article|}}
        {{articles/article-row article=article save="save" articleStates=possibleStates}}
        {{!-- <tr>
          <td>
            {{article.description}}
          </td>
          <td>
            {{article.createdAt}}
          </td>
          <td></td>
          <td></td>
        </tr> --}}
      {{/each}}
    </tbody>
  </table>
{{/if}}
